- name: Provision workstation profile
  hosts: local
  gather_facts: true
  vars_files:
    - ../profiles/workstation.yml
  tasks:
    - name: Compose selected workstation roles
      ansible.builtin.include_role:
        name: "{{ item }}"
      loop: "{{ profile_roles }}"
