---
- name: Provision Noctalia desktop profile
  hosts: local
  gather_facts: true
  vars_files:
    - ../profiles/desktop.yml
    - ../profiles/desktop-noctalia.yml
  pre_tasks:
    - name: Validate selected Arch platform
      ansible.builtin.include_role:
        name: platform_arch
  tasks:
    - name: Compose selected Noctalia desktop roles
      ansible.builtin.include_role:
        name: "{{ profile_role }}"
      loop: "{{ profile_roles }}"
      loop_control:
        loop_var: profile_role
