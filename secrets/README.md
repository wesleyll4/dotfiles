# Secrets

Do not commit secrets, tokens, private keys, credentials, or private files.

The first implementation accepts local ignored files such as `*.local.yml`.
Ansible Vault may be introduced only when a concrete secret needs to be managed.
