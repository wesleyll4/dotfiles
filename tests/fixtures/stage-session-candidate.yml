---
- name: Validate current Hyprland session candidate
  hosts: local
  gather_facts: false
  tasks:
    - name: Run current session candidate role
      ansible.builtin.include_role:
        name: desktop_session_current
